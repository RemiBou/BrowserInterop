{
  "name": "BrowserInterop.e2etests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cypress open",
    "run": "wait-on -l http://localhost:5000 && cypress run",
    "wait-local": "wait-on -l http://localhost:5000",
    "start-server": "cd ../../sample/SampleApp && dotnet run --verbosity quiet /Logging:LogLevel:Microsoft.AspNetCore Warning",
    "test": "docker pull --quiet cypress/included:4.0.2 && docker run --network='host' -v $PWD:/e2e -w /e2e cypress/included:4.0.2 --browser chrome --reporter junit --reporter-options mochaFile=junit-result-[hash].xml && docker run --network='host' -v $PWD:/e2e -w /e2e cypress/included:4.0.2 --browser firefox  --reporter junit --reporter-options mochaFile=junit-result-[hash].xml ",
    "ci": "start-server-and-test start-server http://localhost:5000 test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^4.0.2",
    "start-server-and-test": "^1.10.8",
    "wait-on": "^4.0.0"
  }
}